/*
import 'package:fizmat_app/models/book.dart';
import 'package:flutter/material.dart';



class FizBookItem extends StatelessWidget {
  final FizBookInfo book;
  const FizBookItem({super.key, required this.book});

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        Expanded(
          child: Image.asset(book.imagePath, fit: BoxFit.cover), // 书籍封面图片
        ),
        const SizedBox(height: 5.0),
        Text(
          book.title, // 书籍标题
          style: const TextStyle(fontSize: 14.0), // 文字样式
          textAlign: TextAlign.center, // 文字居中对齐
          overflow: TextOverflow.ellipsis, // 文字溢出处理
        ),
      ],
    );
  }
}

*/
